import { TrucateTxtPipe } from './trucate-txt.pipe';

describe('TrucateTxtPipe', () => {
  it('create an instance', () => {
    const pipe = new TrucateTxtPipe();
    expect(pipe).toBeTruthy();
  });
});
